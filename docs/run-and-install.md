# Run and Install

## Requirements

```bash
python -m pip install -r requirements.txt
```

Minimum dependency:

```text
pygame
```

`tkinter` is also used for native prompts and file dialogs. Some Linux distributions package it separately.

## Run

```bash
python desktop_environment.py
```

On first launch, the shell may generate local runtime data and ask for first-run user/session setup.
